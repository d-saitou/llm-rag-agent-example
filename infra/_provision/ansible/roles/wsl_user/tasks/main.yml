---
- name: ユーザー作成
  ansible.builtin.user:
    name: "{{ wsl_user_name }}"
    password: "{{ wsl_user_password | password_hash('sha512') }}"
    shell: /bin/bash
    groups: sudo
    append: yes

- name: sudoers設定
  ansible.builtin.copy:
    dest: "/etc/sudoers.d/{{ wsl_user_name }}"
    content: "{{ wsl_user_name }} ALL=(ALL) ALL"
    mode: "0440"
