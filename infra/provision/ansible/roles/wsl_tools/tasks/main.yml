---
- name: aptキャッシュ更新
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600

- name: 基本パッケージインストール
  ansible.builtin.apt:
    name:
      - git
      - curl
      - build-essential
    state: present
